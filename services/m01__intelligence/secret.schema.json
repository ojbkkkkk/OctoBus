{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "apiKey": {
      "type": "string",
      "description": "m01 gateway API key, sent as the x-api-key header (ApiKeyAuth)."
    },
    "api_key": {
      "type": "string",
      "description": "Legacy alias for apiKey."
    },
    "apiToken": {
      "type": "string",
      "description": "Optional Bearer JWT, sent as Authorization: Bearer <apiToken>. Used when apiKey is absent."
    },
    "api_token": {
      "type": "string",
      "description": "Legacy alias for apiToken."
    }
  }
}
