{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "username",
    "password"
  ],
  "allOf": [
    {
      "anyOf": [
        { "required": ["aesKey"] },
        { "required": ["aesKeyHex"] },
        { "required": ["aesKeyBase64"] }
      ]
    },
    {
      "anyOf": [
        { "required": ["aesIv"] },
        { "required": ["aesIvHex"] },
        { "required": ["aesIvBase64"] }
      ]
    }
  ],
  "properties": {
    "username": {
      "type": "string",
      "minLength": 1,
      "description": "TopSec WebUI login username."
    },
    "password": {
      "type": "string",
      "minLength": 1,
      "description": "TopSec WebUI login password."
    },
    "aesKey": {
      "type": "string",
      "minLength": 1,
      "description": "AES key as hex, base64, or UTF-8 text. This is the preferred source for V3.7.6 encryption material."
    },
    "aesIv": {
      "type": "string",
      "minLength": 1,
      "description": "AES IV as hex, base64, or UTF-8 text. This is the preferred source for V3.7.6 encryption material."
    },
    "aesKeyHex": {
      "type": "string",
      "minLength": 1,
      "description": "Alias for aesKey when stored as hexadecimal text."
    },
    "aesIvHex": {
      "type": "string",
      "minLength": 1,
      "description": "Alias for aesIv when stored as hexadecimal text."
    },
    "aesKeyBase64": {
      "type": "string",
      "minLength": 1,
      "description": "Alias for aesKey when stored as base64 text."
    },
    "aesIvBase64": {
      "type": "string",
      "minLength": 1,
      "description": "Alias for aesIv when stored as base64 text."
    }
  }
}
