{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "webhook_url": {
      "type": "string",
      "deprecated": true,
      "description": "Deprecated fallback for the DingTalk custom robot webhook URL. Prefer secret.webhook_url."
    },
    "webhookUrl": {
      "type": "string",
      "deprecated": true,
      "description": "Deprecated fallback alias for webhook_url. Prefer secret.webhook_url."
    },
    "webhook": {
      "type": "string",
      "deprecated": true,
      "description": "Deprecated fallback alias for webhook_url. Prefer secret.webhook_url."
    },
    "url": {
      "type": "string",
      "deprecated": true,
      "description": "Deprecated fallback alias for webhook_url. Prefer secret.webhook_url."
    },
    "secret": {
      "type": "string",
      "deprecated": true,
      "description": "Deprecated fallback for the DingTalk signing secret. Prefer secret.secret."
    },
    "dingding_secret": {
      "type": "string",
      "deprecated": true,
      "description": "Deprecated fallback alias for the DingTalk signing secret. Prefer secret.secret."
    },
    "timeoutMs": {
      "type": "integer",
      "minimum": 1,
      "default": 5000,
      "description": "HTTP timeout in milliseconds."
    },
    "timeout_ms": {
      "type": "integer",
      "minimum": 1,
      "description": "Legacy alias for timeoutMs."
    }
  }
}
