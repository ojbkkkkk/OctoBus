{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "authHeaderName": {
      "type": "string",
      "description": "Optional auth header name, for example Authorization or X-Hermes-Token."
    },
    "authHeaderValue": {
      "type": "string",
      "description": "Optional auth header value. This value is redacted from evidence output."
    },
    "webhookHmacSecrets": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "Named HMAC secrets for SendWebhook. Agents pass hmacSecretName instead of the raw secret."
    }
  }
}
